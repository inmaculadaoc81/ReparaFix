const menu=document.querySelector('.menu-toggle'),nav=document.querySelector('.nav');menu?.addEventListener('click',()=>{const open=nav?.classList.toggle('open');menu.setAttribute('aria-expanded',String(Boolean(open)))});const form=document.getElementById('contactForm'),status=document.getElementById('formStatus');function setStatus(m,t=''){if(!status)return;status.textContent=m;status.className=`full status ${t}`.trim()}if(form){form.addEventListener('submit',async e=>{e.preventDefault();if(!form.reportValidity())return;const btn=form.querySelector('button[type=submit]'),old=btn?.innerHTML||'Enviar consulta →';if(btn){btn.disabled=true;btn.textContent='Enviando…'}setStatus('Enviando consulta…','loading');try{const r=await fetch('/api/contacto',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(Object.fromEntries(new FormData(form).entries()))});let d={};try{d=await r.json()}catch{}if(!r.ok)throw new Error(d.code||'EMAIL_SEND_FAILED');form.reset();setStatus('✓ Consulta enviada correctamente. Te responderemos lo antes posible.','success')}catch(err){console.error('ReparaFix formulario:',err);const map={MISSING_ENVIRONMENT_VARIABLES:'La configuración del correo no está completa en Vercel.',GOOGLE_OAUTH_INVALID_GRANT:'La autorización de Google ha caducado o fue revocada.',GOOGLE_OAUTH_INVALID_CLIENT:'Las credenciales OAuth de Google no son válidas.',GMAIL_PERMISSION_DENIED:'Google ha rechazado el permiso para enviar el mensaje.',INVALID_FORM_DATA:'Revisa los campos obligatorios del formulario.'};setStatus(map[err.message]||'No se pudo enviar la consulta. Puedes llamarnos o escribirnos por WhatsApp.','error')}finally{if(btn){btn.disabled=false;btn.innerHTML=old}}})}